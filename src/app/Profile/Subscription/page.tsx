'use client'

import SignUpMarathon from "@/components/pages/profile/_modules/StudentDashboard/components/CompetitionInscription"

const UserSubscription: React.FC = () => {

  return(
    <SignUpMarathon toggleMenu={() => {}} />
  )
};

export default UserSubscription;
