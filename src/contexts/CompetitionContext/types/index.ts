export interface SubscriptionModel {
  id: string; 
  name: string;
  description: string;
  maxMembers: string;
  initialDate: string;
  initialRegistration: string;
  endRegistration: string;
  status: string;
}